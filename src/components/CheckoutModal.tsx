import React from "react";

interface CheckoutModalProps {
  onClose?: () => void;
}

const CheckoutModal: React.FC<CheckoutModalProps> = () => {
  return null; // Stub: WooCommerce + Razorpay integration later
};

export default CheckoutModal;
